// This file handles the /settings route and renders the settings page UI
import SettingsPage from "@/components/screens/settings-page";

export default function Settings() {
  return <SettingsPage />;
}
